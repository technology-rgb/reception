# Copy this to .streamlit/secrets.toml for local PostgreSQL testing.
# On Streamlit Cloud, paste these as secrets in the dashboard instead.

DATABASE_URL = "postgresql://user:password@host:5432/dbname"
ADMIN_PASSWORD = "admin123"
